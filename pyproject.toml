[tool.poetry]
name = "d254"
version = "0.1.0"
description = "required packages for this app"
authors = ["okothouko <o.arnoh@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
